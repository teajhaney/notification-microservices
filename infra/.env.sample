
NODE_ENV=development
LOG_LEVEL=debug

# ==============================================
# DATABASE (PostgreSQL)
# ==============================================
DB_USER=notification_user
DB_PASSWORD=your_secure_postgres_password_here
DB_HOST=localhost
DB_PORT=5432

# Database names (auto-created by init script)
# - postgres (default)
# - user_service_db
# - template_service_db
# - notification_db

# ==============================================
# PGADMIN (Database Management UI)
# ==============================================
# Access at http://localhost:5050
PGADMIN_DEFAULT_EMAIL=admin@notification-system.local
PGADMIN_DEFAULT_PASSWORD=admin_password_here

# ==============================================
# REDIS (Caching & Rate Limiting)
# ==============================================
REDIS_PASSWORD=your_secure_redis_password_here
REDIS_HOST=localhost
REDIS_PORT=6379

# ==============================================
# RABBITMQ (Message Queue)
# ==============================================
# Management UI: http://localhost:15672
RABBITMQ_USER=notification_queue
RABBITMQ_PASSWORD=your_secure_rabbitmq_password_here
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672

# ==============================================
# CONSUL (Service Discovery)
# ==============================================
# UI: http://localhost:8500
CONSUL_HOST=localhost
CONSUL_PORT=8500

# ==============================================
# EMAIL SERVICE - SMTP CONFIGURATION
# ==============================================
# You can use free SMTP services for development/testing:
# - Gmail SMTP (with App Password)
# - Mailtrap (dev/staging testing)
# - Mailgun (free tier: 5,000 emails/month)
# - Brevo/Sendinblue (free tier: 300 emails/day)
# - Elastic Email (free tier: 100 emails/day)

# Gmail SMTP Example (requires App Password, not regular password)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-specific-password
# SMTP_USE_TLS=true

# Mailtrap Example (for testing - catches all emails)
# SMTP_HOST=sandbox.smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=your-mailtrap-username
# SMTP_PASSWORD=your-mailtrap-password
# SMTP_USE_TLS=true

# Mailgun Example (free tier)
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USER=postmaster@your-domain.mailgun.org
# SMTP_PASSWORD=your-mailgun-smtp-password
# SMTP_USE_TLS=true

# Default values (fill these in)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_USE_TLS=true
FROM_EMAIL=noreply@notification-system.local
FROM_NAME=Notification System

# ==============================================
# PUSH NOTIFICATION SERVICE
# ==============================================
# FCM (Firebase Cloud Messaging) - Free
# Get credentials from: https://console.firebase.google.com
# Download JSON file and place in ./credentials/fcm-credentials.json
FCM_CREDENTIALS_PATH=/app/credentials/fcm-credentials.json

# APNS (Apple Push Notification Service) - Free (requires Apple Developer Account)
# Get credentials from: https://developer.apple.com
# Place .p8 file in ./credentials/apns-auth-key.p8
APNS_AUTH_KEY_PATH=/app/credentials/apns-auth-key.p8
APNS_KEY_ID=your-apns-key-id
APNS_TEAM_ID=your-apple-team-id

# ==============================================
# SERVICE PORTS
# ==============================================
API_GATEWAY_PORT=3000
USER_SERVICE_PORT=3001
ORCHESTRATOR_PORT=3002
TEMPLATE_SERVICE_PORT=3003
EMAIL_SERVICE_PORT=3004
PUSH_SERVICE_PORT=3005

# ==============================================
# SECURITY & SECRETS
# ==============================================
# Generate secure random strings for these
# Example: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-here-min-32-chars
JWT_EXPIRES_IN=1h
API_KEY_SECRET=your-api-key-secret-here

# ==============================================
# RATE LIMITING
# ==============================================
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# ==============================================
# MONITORING & OBSERVABILITY (Optional)
# ==============================================
# SENTRY_DSN=your-sentry-dsn-here
# JAEGER_ENDPOINT=http://localhost:14268/api/traces
# PROMETHEUS_PORT=9090

# ==============================================
# NOTES
# ==============================================
# Free Email Services for Development:
# 1. Mailtrap.io - Perfect for testing (catches all emails)
# 2. Gmail - Use App Passwords (enable 2FA first)
# 3. Mailgun - 5,000 free emails/month
# 4. Brevo (formerly Sendinblue) - 300 free emails/day
# 5. Elastic Email - 100 free emails/day
#
# Push Notification Setup:
# 1. FCM is completely free (unlimited notifications)
# 2. APNS is free but requires Apple Developer account ($99/year)
#
# Generate Secure Passwords:
# openssl rand -base64 32